events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location / {
            return 200 'Mock Remote Execution Server - Use BuildBarn or proper RE server';
            add_header Content-Type text/plain;
        }
        
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}